import headerImage from '../image/nrheader.png'; 
import '../App.css';

const Header = () => {
  return (
    <header>
      <img src={headerImage} alt="Header" /> 
    </header>
  );
};

export default Header;
